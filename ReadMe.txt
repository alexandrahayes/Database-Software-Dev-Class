Notes for the project:

A store contains store items in a database which will have a relationship
to the cart. A cart will contain store items however they will be specifics
and will vary amongst users.
A user will have one cart in this case with many items in it, however a cart will
have many store items which provides the relationships between databases.